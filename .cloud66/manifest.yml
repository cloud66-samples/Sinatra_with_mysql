defaults: &defaults
  sinatra:
    configuration:
      custom_build_command: ls -ltr
      custom_deploy_command: bundle exec rake db:migrate
development: 
  <<: *defaults
staging: 
  <<: *defaults
production: 
  <<: *defaults
